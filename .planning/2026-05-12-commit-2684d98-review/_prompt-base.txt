You are a non-interactive code reviewer. Use Read and Write tools. Do not ask questions.

CONTEXT (one paragraph):
You are reviewing git commit 2684d98 in the council-flow repo. The commit introduced a multi-LLM brainstorming sub-phase in skills/plan/SKILL.md and a file-write dispatch contract in references/multi-llm.md (contributor LLMs use their Write tool to land artifacts at agreed paths; every artifact ends with the sentinel `<!-- council-flow:review-complete -->`).

PRIMARY INPUT (this is sufficient for the review — do NOT Read anything else unless your finding requires it):
- Diff: /Users/wiii/Codes/council-flow/.planning/2026-05-12-commit-2684d98-review/_commit-2684d98.patch (582 lines, 6 files, docs-only)

The diff itself includes the full post-commit text of each section under "+ " lines. Read the diff. You should not need to Read the live files in references/ or skills/ — the diff contains everything you need.

If, and ONLY if, a specific finding requires checking current state vs the commit, you may Read one of:
- /Users/wiii/Codes/council-flow/references/multi-llm.md
- /Users/wiii/Codes/council-flow/skills/plan/SKILL.md
But default to "I have enough from the diff alone."

REVIEW FOCUS:
- Internal consistency of the file-write contract (does the prose actually compose into a working pipeline?)
- CLI assumption realism (does every named CLI actually expose a Write tool in non-interactive mode? What flags are required?)
- Failure-mode coverage in post-call verification (do the 5 checks catch agent-loop hang, partial write, mid-stream cutoff?)
- Operational gotchas (shell quoting, heredoc, stdin behavior, agent permission approval)
- Documentation rot (stale model IDs, broken paths, internal contradictions)

OUTPUT REQUIREMENTS:
1. Write your review using your Write tool to: REPLACE_ME_OUTPUT_PATH
2. The LAST LINE of the file MUST be exactly:
     <!-- council-flow:review-complete -->
3. Print only: "wrote <filename>"
4. Do NOT modify any source files. Read-only review.

OUTPUT FORMAT (Markdown, English):

## Top-level summary
2-3 sentences. Overall quality, any contract-design blockers.

## Inline findings
For each issue:

### <file path>:<line number>
**Severity:** CRITICAL | MAJOR | MINOR | NIT | QUESTION
**Headline:** <one line>

<body — what is wrong, why, what to change>

Use post-commit line numbers. Range findings: "L<start>-L<end>", cite the start line.

## Verdict
One line: "merge as-is" | "merge after minor edits" | "request changes".
